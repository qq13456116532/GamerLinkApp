<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:GamerLinkApp.ViewModels"  
             x:Class="GamerLinkApp.Views.ServiceListPage"
             Title="服务列表">


    <CollectionView ItemsSource="{Binding Services}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <VerticalStackLayout Padding="10">
                    <Label Text="{Binding Title}" FontSize="18" FontAttributes="Bold"/>
                    <Label Text="{Binding GameName}" FontSize="14"/>
                    <Label Text="{Binding Price, StringFormat='价格: {0} 元'}" FontSize="14"/>
                    <Label Text="{Binding ServiceType}" FontSize="14" TextColor="Gray"/>
                </VerticalStackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>